<%= league_team_breadcrumbs(@league, @team) %>
<%= enable_page_title "#{@team.name} Roster Options" %>

<div class="page-header">
  <h1>Roster Options</h1>
</div>

<%= render 'form', roster: @roster, team: @team %>

<p>
  <%= link_to 'Back', team_rosters_path(@team) %>
</p>
