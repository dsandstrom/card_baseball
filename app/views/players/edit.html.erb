<%= player_breadcrumbs(@player) %>
<%= enable_page_title "#{@player.roster_name} Settings" %>

<div class="page-header">
  <div class="columns">
    <div class="first-column">
      <%= content_tag :h1, 'Player Settings' %>
    </div>
  </div>
</div>

<%= render 'form', player: @player %>
